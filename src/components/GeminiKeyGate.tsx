// Placeholder — the previous key-entry dialog is removed. All AI calls use
// the server-side GEMINI_API_KEY via the gemini-proxy edge function.
export default function GeminiKeyGate() { return null; }
